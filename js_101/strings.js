const text = "   This is sample text    ";

// How to get length of string
// console.log(text.length);

// You can access characters by their index and you can also update characters by their index
// const firstCharacter = text[0];
// console.log(firstCharacter);

// text[0] = "P";
// console.log(text);

// How to convert string to upper case
// console.log(text.toUpperCase());

//How to convert string to lower case
// console.log(text.toLowerCase());

// We can split our text by specific charter
// For example we can split string to words with .split(" ")
// console.log(text.split(" "));
// The character by which we are splitting our array will not be included
// console.log(text.split("i"));
// const array = text.split("i");

//We can also join the array back
// console.log(array.join("i"));

// We can take part of our text out
// If we specify just start it will keep everything after the start
// If we specify the end as well it will keep only the part between start and finish
// console.log(text.slice(2, 4));

// We can remove with spaces from start and end
// White space is for example space or any other invisible character
// console.log(text);
// console.log(text.trim());
// You can also trim just start or the end
// console.log(text.trimEnd());
// console.log(text.trimStart());

// This is how you can also take part of your string and remove the rest
// console.log(text.substr(2, 10));

// console.log(text);
